# נקודת חזרה — לפני מעבר להתראות שיחה

נשמר ב-21 באוגוסט 2026, לפני ההרצה שמחליפה את החלון הקופץ
בהתראת מערכת.

---

## המצב שרץ עכשיו

| | |
|---|---|
| גרסת ייצור | `c5c61cd0c4363859a431eeb2cf93461e2d19049e` |
| נפרסה | 20/08/2026 23:45 |

**הכתובת ב-3CX כרגע:**

```
https://talmud-tora-web.onrender.com/phone/pop?number=%CallerNumber%
```

**איך זה מתנהג:** נפתחת לשונית קצרה, ומיד אחריה חלון קטן עם כרטיס
המתקשר. יש הבזק של לשונית ביניים.

---

## החזרה — הדרך הפשוטה

**זה כל מה שצריך: להחזיר את הכתובת ב-3CX.**

באפליקציית 3CX ← הגדרות ← Integration ← Open Contact URL:

```
https://talmud-tora-web.onrender.com/phone/pop?number=%CallerNumber%
```

**זהו.** מסלול `/phone/pop` נשאר במערכת ולא הוסר — הוא ממשיך לעבוד
בדיוק כמו קודם.

> **אין צורך להחזיר קוד, אין צורך בפריסה, ואין צורך לגעת במסד.**
> ההתראות פשוט לא ייקראו על ידי אף אחד.

---

## אם בכל זאת רוצים להסיר גם את הקוד

לא נדרש — אבל אם תרצו לנקות:

1. למחוק את `server/public/js/call-notify.js`
2. להסיר מ-`server/views/partials/footer.ejs` את שורת הטעינה שלו
3. הטבלה `incoming_calls` יכולה להישאר; היא מנקה את עצמה כל 24 שעות
   ותישאר ריקה אם אף אחד לא פונה ל-`/phone/ring`.

---

## חזרה מלאה לגרסת הייצור הנוכחית

רק אם משהו חמור נשבר. **ההרצה מוחקת שינויים מקומיים:**

```
git fetch origin
git reset --hard c5c61cd0c4363859a431eeb2cf93461e2d19049e
git push --force origin main
```

**זהירות:** פעולה זו מחזירה את **כל** הקוד ליום 20/08 בשעה 23:45,
כולל תיקונים אחרים שנעשו אחריה. עדיף להחזיר רק את הכתובת ב-3CX.

**המסד אינו מושפע** — הוא על דיסק קבוע ואינו חלק מהפריסה.

---

## שתי הדרכים זו לצד זו

| | חלון קופץ (`/phone/pop`) | התראה (`/phone/ring`) |
|---|---|---|
| הבזק לשונית | יש | אין |
| פתיחה | אוטומטית | בלחיצה על ההתראה |
| כשהמערכת ממוזערת | לא עובד | עובד |
| בנייד | לא | כן |
| דורש אישור התראות | לא | כן, פעם אחת |

**שתיהן קיימות במערכת במקביל.** המעבר ביניהן הוא שינוי כתובת אחת
ב-3CX, בכל מחשב בנפרד — אפשר גם לתת למזכיר אחד לנסות ולהשאיר
את השאר על הישן.
