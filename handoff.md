# handoff.md — תלמוד תורה החדש

מסמך המשכיות. **בשיחה חדשה: העלה את הקובץ הזה בהודעה הראשונה.** הוא נועד
לתת ל-Claude תמונה מלאה תוך דקה, בלי לעבור על תמלילים.

עודכן: 20 באוגוסט 2026

---

## תשתית

| | |
|---|---|
| מאגר | `https://github.com/tatyesod/talmud-tora-web` |
| חי | `https://talmud-tora-web.onrender.com` |
| מחסנית | Node + Express + EJS + SQLite (`node:sqlite`) |
| אירוח | Render, מופע Standard 25$ לחודש |
| דיסק קבוע | 1 GB ב-`/var/data`, המסד ב-`/var/data/talmud-tora.db` |
| היקף | 837 תלמידים פעילים, 647 משפחות, 3 סניפים |
| סניפים | סוקולוב, בן פתחיה, נפחא |

**זרימת עדכון:** Claude מייצר ZIP עם עוטף `app/` → המשתמש מריץ `update.ps1`
מ-Windows → git push → Render פורס אוטומטית.
`_delete.txt` בשורש ה-ZIP מוחק קבצים בייצור (נתיב לשורה).

**המשתמש:** דוד. מנהל וגם המפתח היחיד. עובד בעברית. **תמיד לענות בעברית.**

---

## מוסכמות מחייבות

1. **עברית מיושרת לימין** בכל מקום, אלא אם נאמר אחרת. בס"ד תמיד בימין למעלה.
2. **סדר הכיתות:** מכינה א' → מכינה ב' → כיתה א' … כיתה ח'. הקבוע `GRADE_ORDER`
   ב-`yearManager.js`, ועמודה `classes.grade_order`.
3. תאריכים בתצוגה: DD/MM/YYYY. שנה עברית (תשפ"ז) לשיוך נתונים.
4. **רשימות לבנות של שדות** (`STUDENT_FIELDS`, `TEACHER_FIELDS`, ורשימת
   `families.js`) **מפילות בשקט** כל שדה שאינו בהן. שדה חדש → להוסיף גם שם.

---

## מה נבנה ב-20 באוגוסט (השיחה האחרונה)

### הרשאות לפי תפקיד
`ROLE_RULES` ב-`index.js`, בראש הקובץ (חייב להיות לפני השימוש).
- `maintenance` — מסך בקשות תחזוקה בלבד (תחזוקן חיצוני, טאבלט מסונן).
  **המשתמש ביקש לא לשקלל את הטאבלט בשיקולי עיצוב.**
- `pedagogic` — רכזת שילוב: תלמידים, כיתות, משפחות, דוחות. עריכה מלאה.
  בלי כספים, מלמדים, מלאי, גיבויים, ניהול שנה, משתמשים.
  **ארבעה כרטיסים בדף הבית בלבד** (`cards` ב-ROLE_RULES).
- `canAccess()` בתצוגות מסתירה תפריט וכרטיסים; `showCard()` נפרדת ממנה.
- **מלכודת:** `"/"` ברשימה המותרת פותח את כל המערכת אם משווים ב-startsWith.
  מטופל כהתאמה מדויקת בלבד.

### מודול סריקות (`/scans`)
מסך "הצהרת בריאות" בדף הבית ובתפריט.
- **קוד QR מודפס על הטופס** — תוכן `HD|<student_id>|<שנה>|<עמוד>`.
  מאפשר שיוך אוטומטי מלא בסריקה. מ-2027 זו הדרך העיקרית.
- **שיוך ידני** לטפסים שהודפסו לפני הקוד (כל טפסי תשפ"ז):
  `/scans/manual` (מעורב, כל הכיתות) ו-`/scans/class/:id/manual` (כיתה אחת).
  **השלמה אוטומטית:** מזהים טופס אחד → הכיתה ידועה → כל השאר מתמלא לפי
  סדר ההדפסה. תלמיד שלא החזיר מסיט את הסדר, והתמונה חושפת זאת.
- הפיצול, קריאת הקודים והציור — **הכל בדפדפן**, לא בשרת.
  ספריות מקומיות ב-`server/public/js/vendor/` (לא CDN).
- **`pdfjs` v6 דורש `standardFontDataUrl` ו-`wasmUrl`.** בלעדיהם סריקה
  מצוירת דהויה עד היעלמות. זה עלה שעה לאבחן.
- מניעת כפילות בשתי רמות: אותו קובץ (hash), ואותו תלמיד+שנה (אילוץ במסד).
- ייצוא כיתה לקובץ אחד, ממוין לפי א-ב.
- ניקוי שנים קודמות מוחק **רק** טפסים שכבר הוחלפו.
- מסך צפייה `/scans/view/:id` — **מצייר את ה-PDF בעצמו** ולא במציג הדפדפן,
  כי ההגדרה "הורד PDF במקום לפתוח" שוברת מסגרת מוטמעת.

### גילוי עדכוני הורים
**התובנה:** אין צורך לקרוא כתב יד. המערכת יודעת אילו שדות **הדפיסה ריקים**,
ובודקת אם יש בהם דיו. שדה ריק ≈ 0.3% פיקסלים כהים; שדה שמולא 1.7%-10%.
סף: 1.2%. **נבדק על טופס אמיתי: 7/7, אפס החמצות ואפס התרעות שווא.**
- `server/public/js/scan-fields.js` — מפת השדות והגילוי.
- `/scans/updates` — מסך שמציג **שדות בודדים** עם תמונת מה שנכתב,
  הערך הקיים, ותיבת הקלדה. נשמר לכרטיס המשפחה/התלמיד.
- טבלה: `scan_findings`.

### טופס הצהרת בריאות
צומצם **משני עמודים לאחד** (837 תלמידים = 837 עמודים במקום 1674).
גופן 8.5pt, שוליים 5×10 מ"מ, פסקאות משפטיות מוקטנות בנפרד.
נמדד: 14 מ"מ מרווח בתחתית.
- **אין `overflow:hidden` על `.page` בכוונה** — עדיף עמוד שני גלוי מאשר
  טבלת חתימות שנחתכת בשקט.
- שדה "הליכה עצמאית" נוסף (היה בכרטיס התלמיד ומעולם לא התעדכן מההורים).
- תיבות סימון הן **ריבועים מצוירים**, לא תווי טקסט — תו ☐ מוצג שונה בכל
  מדפסת ולעתים כלל לא.

### אינטגרציית 3CX
- **שיחה נכנסת:** באפליקציית 3CX → Settings → Integration →
  "Open Contact in Custom CRM", ובשדה הכתובת:
  **`web+ttcall:%CallerNumber%`** — בלי לוכסנים!
  **הלוכסנים `://` נחסמים על ידי חומת האש של Render (403).** זה נמדד.
- `protocol_handlers` ב-`manifest.json` רושם את `web+ttcall`, כדי שייפתח
  באפליקציה המותקנת ולא בדפדפן. דורש התקנה מחדש של האפליקציה.
- `launch_handler: navigate-new` — **חלון נפרד לכל שיחה**. היה
  `navigate-existing`, וזה גרם לכרטיס להשתלט על החלון שעובדים בו.
- **שיחה משלוחה פנימית:** 3CX שולחת מספר קצר (למשל 203). `findByExtension`
  מזהה את הכיתה לפי `classes.extension` ובוחר מלמד **לפי שעת השיחה** —
  לפני 14:00 בוקר, אחריה אחה"צ. **שני המלמדים מוצגים תמיד** והצפוי מסומן,
  כי מלמד הבוקר עשוי להישאר אחה"צ. השעה מ-`hd.israelHour()` ולא מהשרת,
  כי Render רץ ב-UTC. שדה השלוחה נערך בטופס הכיתה וב-`/classes/extensions`.
- השיחה נפתחת ב-`phone/popup.ejs` — מסך עצמאי וקטן (כ-4KB), בלי תפריט
  ובלי מסגרת המערכת, עם שמות הילדים והכיתות. הכרטיס המלא נפתח מתוכו
  בלשונית נפרדת. `resizeTo` מקטין את החלון אם הדפדפן מרשה.
- `/phone/incoming` — נרמול מספרים: 7 ספרות אחרונות, מסיר קידומת 972.
  נבדק בשישה פורמטים. 94% מהמשפחות מזוהות.
- לחיצה על מספר במערכת: מעתיקה ללוח **וגם** מנסה לחייג דרך 3CX.

### שדה מקום תפילה
בייבוא מ-Access הייתה עמודה אחת "מקום עבודת האב" ולתוכה הוזנו גם כוללים
וגם בתי כנסת. נוסף `families.father_synagogue`. מסך הסיווג החד-פעמי הוסר
אחרי השימוש (ב-`_delete.txt`).

---

### העלאת שנה — מה קורה בפועל (נסרק ואומת)

`yearManager.promoteYear()`, חמישה שלבים:
1. **תמונת מצב** של השנה היוצאת נשמרת — זה מה שמאפשר סינון לפי שנה.
2. **קידום כל תלמיד:** כיתה ח' → ארכיון עם `archive_type='בוגר'`;
   כיתת המתנה → מכינה א' באותה מקבילה **והסטטוס הופך ל"פעיל"**;
   השאר → הכיתה הבאה ב-`GRADE_ORDER`.
3. **שיבוץ אוטומטי לפי אזור** לתלמידים ללא כיתה (אחים קטנים): קודם לפי
   אח פעיל שכבר לומד, ורק אם אין — לפי כתובת.
4. **איפוס `classes.transfer_number`** — החריגות תקפות להעלאה אחת בלבד.
5. העלאת מספר השנה ב-`settings`.

**מסך מפת המעברים:** `/year/transfers` — כל כיתה ולאן היא עולה, בורר
מקבילה לחריגות, ואזהרה כשמקבילת היעד אינה קיימת. אומת שהמפה חוזה את
ההעלאה במדויק על 32 כיתות, כולל חריגה ידנית.

**`classes.class_kind`** (`regular` / `waiting`) החליף זיהוי לפי טקסט בשם.
הזיהוי הישן היה שביר: שינוי קל בשם היה משתק את ההעלאה בשקט.


### לוח שנת הלימודים (`/year-calendar`)

משימות פנימיות שחוזרות כל שנה **בתאריך עברי** — רישום חדשים בכ"ח חשון,
עדכון שכ"ל בא' אב, הצהרות בריאות באלול. נפרד ממודול האירועים, שהוא
אירועים שההורים יודעים עליהם.

- טבלאות: `year_tasks` (חודש+יום עברי, ימי תזכורת, קישור לפעולה)
  ו-`year_task_done` (ביצוע **לכל שנה בנפרד**, כדי שההיסטוריה נשמרת).
- **מספור החודשים ב-`hebrewDate` מתחיל בניסן (1), ותשרי הוא 7.** נבדק
  בפועל. התצוגה לפי סדר שנת הלימודים (אלול ראשון) דרך `SCHOOL_ORDER`.
- משימה שמועדה חלף מצביעה אוטומטית **לשנה הבאה**; אם לא סומנה כבוצעה,
  מוצג "המועד השנה חלף".
- **הזנת יומן:** `/year-calendar/feed.ics` — מוסיפים פעם אחת ליומן גוגל
  (הוספת יומן ← מכתובת URL). חד-כיווני, בלי אימות, ולכן כותרות בלבד.
- סימון "בוצע" הוא **ידני בכוונה** — לכל משימה מדד אחר.

## פתוח

0. **כיול מחדש של גילוי הדיו לפני סבב 2027.** `scan-fields.js` מכויל
   לקואורדינטות של פריסת הטופס **כפי שהודפסה בתשפ"ז**. הפריסה שונתה אחריה
   (רוחבי העמודות ב-`colgroup` מ-15/35 ל-19/31, וריווח השורות).
   הגילוי ימשיך לעבוד על סריקות תשפ"ז, אבל **הטפסים הבאים יידרשו כיול**:
   להדפיס טופס, לסרוק, למדוד את מרכזי השורות ואת תחומי העמודות, ולעדכן
   `ROW0`, `STEP`, `RIGHT`, `LEFT`.

1. ~~סדר גילאי הכיתות~~ — **הושלם.** כל המיונים עברו ל-`grade_order`
   (25 שאילתות בסבב האחרון, על כל דפוסי הניסוח). אומת בפועל:
   מכינה א' → מכינה ב' → כיתה א'…, וכיתות ההמתנה בסוף.
2. ~~הסתרת "עדיין לא נכנסו"~~ — **היה כבר מיושם**: 15 מסננים
   `NOT LIKE 'עדיין לא נכנסו%'` בשישה קבצים.
   **שתי אזהרות:** ב-`letters.js` הכיתות האלה נכללות **בכוונה** כיעד
   שיבוץ. ושני תלמידים פעילים יושבים בכיתת המתנה אחת — הסתרה גורפת
   הייתה מעלימה אותם מהמערכת.
3. `users.nav_order` — עמודה שנשארה בשימוש (סדר כרטיסי דף הבית בגרירה).

---

## לקחים שנצרבו בדם

**גרש עברי שובר HTML.** `data-x='<%- JSON.stringify(...) %>'` נשבר כשהערך
מכיל גרש — ושמות הכיתות שלנו הם "מכינה א'". התוצאה: הסקריפט קורס בשורה
הראשונה והכפתורים נראים מתים. **נתונים ל-JS עוברים בבלוק
`<script type="application/json">`, לא במאפיין.**

**git מציג תיקייה חדשה כשורה אחת.** אריזה לפי `git status` מחמיצה את כל
הקבצים שבתוכה. **תמיד:**
`git ls-files --modified` + `git ls-files --others --exclude-standard`.
זה הפיל את הייצור פעם אחת (קבצי תצוגה חסרים → 500).

**`git checkout package.json` מוחק תלויות אמיתיות** שהותקנו בשיחה.
אחרי ניקוי חבילות זמניות — לוודא ש-`qrcode`, `pdf-lib`, `jsqr`, `pdfjs-dist`
עדיין רשומות.

**שני גוונים לכותרות טבלה.** טבלה רגילה `#efe9da`; טבלה עם גלילה
(`.table-scroll-wrap thead th`) `#e8ecf0`. אי-התאמה נראית כמו באג צבע.

**עריכת הודעה והרצה חוזרת משאירה קבצים על הדיסק** מהריצה הקודמת, והם
נכנסים לחבילה. לאמת כל קובץ בדיף מול מה שנערך בפועל.

**`min-height` בהדפסה יוצר עמוד ריק.** להגביל ל-`@media screen`.

**סדר מסלולים ב-Express:** נתיב קבוע חייב לבוא **לפני** `/:id`.

**`db` נטען מקומית בכל מסלול ב-`index.js`**, לא ברמת הקובץ.

**מיקום מוחלט "בתחתית העמוד" אינו אמין** כשהתוכן עלול לגלוש — התחתית
עוברת לדף הבא. עדיף זרימה רגילה.

**חיפוש `<body>` נכשל אם התג נכתב `<body >`.** לחתוך לפי מבנה תוכן.

---

## הרגלים שהמשתמש מצפה להם

- **לבדוק לפני שאומרים שמשהו עובד.** להריץ שרת, למדוד, לא להעריך.
- **להודות בטעות מיד ובמפורש**, ולומר מה בדיוק נשבר ולמה.
- **לומר מה לא נבדק.** הוא מעדיף "לא הוכחתי" על ביטחון מזויף.
- לארוז ZIP אחרי כל שינוי, ולציין את מספר הקבצים.
- להציע את הצעד הבא, לא לחכות להוראה.
